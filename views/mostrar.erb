<DOCTYPE html>
<html>
  <head>
  		<title>El Blog Iron Hack</title>
  		<link rel="stylesheet" href="./public/css/blog.css" type="text/css">
  		<meta charset="utf-8">
  </head>
  <body>
    <header>
      <h1>Blog Iron Hack</h1>
    </header>
    <br>
    <!-- <% @posts.each_with_index do |post, index|%>
    <p><a href="/post_details/<%=index%>"><%= post.title %></a></p>
    <% end %> -->
    <!-- <% @posts.each do |post|%>
    <% array_palabra= post.title.split("")%>
    <% enlace="" %>
      <%array_palabra.each do |caracter|%>
      <%   if caracter==" "%>
        <%    caracter ="_"%>
      <%   end   %>
      <%   enlace << caracter %>
      <% end   %>

    <p><a href="/post_details/<%=enlace%>"><%= post.title %></a></p>
    <% end %> -->
    <%@posts.each do |post|%>
    <p><a href="/post_details/<%=post.title.downcase.gsub(" ","_")%>"><%= post.title %></a></p>
    <% post.link = post.title.downcase.gsub(" ","_") %>
    <%end%>
    <form method = "get" action = "/new_post">
    <input type = "hidden" name = "el_id" value = "" >
    <button type = "submit" name = "completar"> Nuevo Post </button>
    </form>
</body>
</html>
